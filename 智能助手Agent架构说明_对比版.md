# VideoStudio Pro 智能助手 Agent 架构说明 - 对比版

> 这份文件用于对比不同写法，不代表最终定稿。
> 重点是梳理 Agent 架构、知识库、Memory、回复格式和技能模块。

---

## 版本 A：结构说明版

这一版偏架构说明，适合先把系统拆清楚。

---

### 1. 回复格式示例

这一部分放在前面，用来先说明智能助手应该如何回答。

#### 1.1 平台使用问答

当用户问平台怎么用时，优先使用这种格式：

```text
我理解你的目标是：
...

建议你这样做：
1. ...
2. ...
3. ...

下一步可以继续：
...
```

示例：

```text
我理解你的目标是：想从一个产品广告创意开始，逐步做出图文视频方案。

建议你这样做：
1. 先在文本节点里写一句核心创意。
2. 用扩写功能把创意变成短片脚本。
3. 把脚本拆成 3-5 个关键分镜。
4. 为每个分镜生成视觉参考图。
5. 再选择关键画面推进到视频节点。

下一步可以先输入产品名称、目标用户和想要的视觉气质。
```

#### 1.2 创作生成

当用户要求生成脚本、分镜、提示词或视觉方案时，优先直接给结果。

```text
下面是一版可以直接放进画布文本节点的内容：
...
```

示例：

```text
下面是一版可以直接放进画布文本节点的分镜草案：

镜头 1：黑色背景中，一束冷光扫过产品边缘，突出材质。
镜头 2：镜头缓慢推进，产品从阴影中显现，形成高级感。
镜头 3：局部微距展示品牌 Logo 和核心细节。
镜头 4：产品与使用场景结合，传达生活方式。
镜头 5：画面收束到品牌口号和主视觉。
```

#### 1.3 提示词优化

当用户对生成效果不满意时，优先指出问题，再给可直接使用的新提示词。

```text
问题可能在：
- ...

建议增强：
- ...

可直接使用的提示词：
...
```

示例：

```text
问题可能在：
- 主体描述太宽泛
- 镜头语言不够明确
- 光线和材质没有被约束

建议增强：
- 明确主体位置
- 增加景别和镜头运动
- 固定色彩、材质和广告风格

可直接使用的提示词：
高端香水瓶置于黑色镜面台面，冷调蓝色边缘光，浅景深，微距产品摄影，奢侈品广告风格，背景干净，主体居中，玻璃材质清晰，电影级光影。
```

#### 1.4 vibecoding 检测

当用户要求判断功能或代码是否有 vibecoding 倾向时，使用这种格式：

```text
总体判断：低 / 中 / 高

主要问题：
- ...

做得好的地方：
- ...

优先修改：
1. ...
2. ...
3. ...
```

#### 1.5 开发协作

当用户问代码在哪里改、功能怎么接入时，使用这种格式：

```text
结论：
...

涉及位置：
- ...

修改思路：
1. ...
2. ...
3. ...

注意事项：
- ...
```

---

### 2. 技能模块设计

技能模块用于把智能助手的能力拆成可组合的任务。

#### 技能一：创意扩写

适用场景：

- 用户只有一句创意
- 用户想做短片脚本
- 用户想把文字节点扩展成更完整的内容

输入：

- 用户原始创意
- 当前文本节点内容
- 选择的风格或题材

输出：

- 更完整的故事段落
- 场景描述
- 镜头氛围
- 可继续拆分镜的文本

回复重点：

- 不要只增加字数
- 要补充画面、动作、节奏和情绪
- 输出应能继续进入分镜或图片生成

#### 技能二：分镜拆解

适用场景：

- 用户已有故事或脚本
- 用户想生成一组镜头
- 用户想把文本推进到图片节点

输入：

- 脚本内容
- 目标镜头数量
- 风格方向
- 产品或角色信息

输出：

- 镜头编号
- 画面内容
- 景别
- 镜头运动
- 光影氛围
- 图片生成提示词片段

回复重点：

- 每个镜头要能单独生成画面
- 镜头之间要有节奏推进
- 不要只写剧情，要写清楚画面

#### 技能三：提示词优化

适用场景：

- 用户觉得生成结果不稳定
- 用户想把中文描述转成更适合图像生成的 prompt
- 用户想统一画面风格

输入：

- 原始提示词
- 用户不满意的点
- 参考图或风格要求

输出：

- 问题分析
- 优化方向
- 可直接使用的新提示词
- 可选负面约束

回复重点：

- 明确主体、构图、光线、材质、镜头和风格
- 尽量减少模糊词
- 让提示词更稳定、更可控

#### 技能四：品牌风格统一

适用场景：

- 用户上传品牌图、Logo 或产品图
- 用户希望多张画面风格一致
- 用户想形成品牌视觉预设

输入：

- 品牌参考图
- 产品图
- 用户想要的视觉气质
- 当前画布中已有图片节点

输出：

- 品牌色建议
- 统一构图规则
- 光影和材质约束
- 后续生成应重复使用的关键词

回复重点：

- 保持主体一致
- 保持色彩和材质一致
- 保持镜头语言一致
- 避免每张图都像不同项目

#### 技能五：vibecoding 检测

适用场景：

- 用户担心功能只是看起来能跑
- 用户想检查产品或代码结构是否合理
- 用户想知道下一步该先修哪里

输入：

- 功能描述
- 代码片段
- 当前实现方式
- 用户担心的问题

输出：

- 总体判断
- 主要问题
- 做得好的地方
- 优先修改建议

回复重点：

- 判断是否有真实闭环
- 判断前后端职责是否清楚
- 判断提示词和数据流是否稳定
- 给出最优先的 3 个修改点

---

### 3. 这是什么

VideoStudio Pro 的智能助手不是一个简单的聊天框，而是一个围绕 AI 影视创作平台设计的 Agent 系统。

它的目标是帮助用户在一个平台里完成：

- 创意构思
- 画布节点操作
- 提示词优化
- 知识库问答
- 图片生成
- 工程检查
- 后续功能迭代

它不只是回答“用户问了什么”，而是要理解：

- 用户现在想做什么
- 当前画布上有什么内容
- 当前项目处于什么阶段
- 应该参考哪类知识
- 应该调用哪个模型或工具
- 结果应该返回到哪里

---

### 4. 整体架构

智能助手可以拆成三层：

```text
输入层 Input Layer
↓
决策层 Agent Brain
↓
执行层 Execution Layer
```

这三层分别负责：

- 输入层：收集用户、画布、素材和上下文
- 决策层：判断意图、选择知识库、调度 Memory、组合 Prompt
- 执行层：调用模型、查知识库、生成结果、更新画布或项目文件

---

### 5. 输入层 Input Layer

输入层负责收集所有可能影响判断的信息。

#### 5.1 用户自然语言输入

用户可以直接输入：

- 创作需求
- 平台使用问题
- 代码修改需求
- 提示词设计问题
- vibecoding 检测需求
- 图片生成需求
- UI 或交互设计建议

例如：

```text
帮我把这个想法扩写成一个短片脚本。
```

```text
参考这张图片生成相似的风格。
```

```text
右侧助手的提示词在哪里改？
```

#### 5.2 画布上下文输入

平台的核心界面是无限画布，因此智能助手需要理解画布状态。

画布上下文可以包括：

- 当前选中的文本节点
- 当前选中的图像节点
- 当前选中的视频节点
- 节点之间的连接关系
- 当前工作流阶段
- 当前选择的风格或模型

#### 5.3 素材输入

用户可能上传或选择：

- 品牌参考图
- Logo
- 产品图
- 角色图
- 场景图
- 视频素材

这些素材会成为生成图片、统一风格、做品牌风格智控的重要输入。

#### 5.4 会话上下文输入

智能助手还需要知道用户刚刚说过什么。

这部分属于短期 Memory，例如：

- 当前对话正在讨论什么
- 用户刚刚纠正了什么
- 用户当前想要的输出格式
- 用户是否只是想看方案，而不是立刻改代码

---

### 6. 决策层 Agent Brain

决策层是整个智能助手的核心。

它负责判断用户真正想做什么，并决定下一步应该使用哪类知识、哪个 Prompt、哪个模型或哪个工具。

#### 6.1 意图识别 Intent Detection

常见意图包括：

- 产品问答
- 创作生成
- 代码定位
- UI 设计建议
- Prompt 设计
- vibecoding 检测
- 图片生成
- 项目工程修改
- Agent 架构讨论

#### 6.2 知识库路由 Knowledge Routing

知识库路由不是让用户理解内部技术结构，而是让智能助手在实际使用时自动判断“应该参考哪类资料”。

用户只需要正常提问，系统在后台完成判断。

例如：

```text
我想做一个产品广告短片，应该从哪里开始？
```

这时应优先参考创作流程和平台功能，告诉用户如何从文本节点、视觉参考、视频节点一步步推进。

```text
我上传了品牌参考图，怎么保持后面的画面风格一致？
```

这时应优先参考品牌风格智控和素材引用，解释如何使用品牌图、Logo、产品图来统一视觉调性。

```text
这个生成结果不稳定，应该怎么优化提示词？
```

这时应优先参考提示词优化和模型使用经验，帮助用户调整描述方式、风格词和约束条件。

知识库路由的作用是：

- 用户不用知道背后有多少个知识库
- 用户不用自己选择使用哪类知识
- 助手会根据问题自动找到最相关的资料
- 回答会尽量贴近当前创作场景

#### 6.3 Memory 调度 Memory Manager

Memory 用来让助手保持上下文，而不是每次都像第一次看到项目。

可以分成三类：

- 短期 Memory：当前对话里的上下文
- 中期 Memory：当前项目阶段内持续有效的信息
- 长期 Memory：用户长期偏好

#### 6.4 参数补全 Slot Filling

当用户表达不完整时，决策层需要判断是否追问。

例如用户说：

```text
帮我改一下这个。
```

但没有说明“这个”是什么，就需要补全信息。

可能要追问：

- 是改 UI 还是改后端？
- 是只出方案，还是直接改代码？
- 是修改右侧助手，还是修改画布节点？
- 是要生成文字，还是生成图片？

#### 6.5 Prompt 组合 Prompt Composer

最终发给模型的内容不应该只有用户一句话。

更合理的结构是：

```text
system prompt
+ 当前任务类型
+ 相关知识库摘要
+ Memory 摘要
+ 用户当前输入
+ 输出格式要求
```

---

### 7. 执行层 Execution Layer

执行层负责真正做事。

#### 7.1 文本模型执行

文本模型可以用于：

- 知识库问答
- 剧本扩写
- 文案润色
- 提示词优化
- UI 设计建议
- vibecoding 检测
- Agent 架构解释

#### 7.2 图片模型执行

图片模型可以用于：

- 文生图
- 参考图生成
- 视觉方案生成
- 架构图生成
- 思维导图生成
- UI 概念图生成

#### 7.3 工程动作执行

当用户希望修改项目时，执行层可以进行工程动作。

包括：

- 查找文件
- 阅读代码
- 修改代码
- 新增组件
- 调整 Worker
- 运行构建
- 检查报错
- 准备部署

#### 7.4 画布动作执行

未来如果智能助手和画布深度联动，可以执行：

- 创建文本节点
- 创建图片节点
- 创建视频节点
- 更新节点内容
- 连接节点关系
- 应用品牌风格参数
- 把生成结果放回画布

---

### 8. 当前项目里的对应关系

当前项目中已有这些基础模块：

```text
src/App.jsx
```

主应用，包含无限画布、节点、工具栏、品牌风格智控等核心 UI。

```text
src/VideoStudioAssistant.jsx
```

右侧智能助手面板，负责聊天输入、快捷按钮、模型选择和请求发送。

```text
worker.js
```

Cloudflare Worker 后端，负责接收前端请求、选择 system prompt、调用模型并返回结果。

```text
KNOWLEDGE_BASE.md
```

项目知识库，记录平台功能、技术栈、部署方式、模型映射等。

```text
ASSISTANT_DOCS.md
```

助手说明文档，记录前端代码位置、API 调用流程、提示词字段位置等。

当前需要补齐的是：

- 更完整的知识库路由
- 更明确的 actionType 设计
- 短期 / 中期 / 长期 Memory 说明
- 面向智能助手的 system prompt
- 知识库真正接入 Worker 问答流程

---

## 版本 B：平台调性版

这一版更偏 VideoStudio Pro 自己的创作语境，适合往产品介绍或正式说明方向继续打磨。

### 1. 多模态创作输入层

VideoStudio Pro 的输入不是单一聊天文本，而是围绕“创作现场”收集信息。

需要同时理解：

- 用户语言
- 画布节点
- 上传素材
- 模型选择
- 当前创作阶段

用户可能在找选题、扩写脚本、拆分镜、统一风格、优化提示词，也可能在问项目实现。

### 2. 创作意图决策层

决策层需要判断用户当前想推进哪件事：

- 选题与创意方向
- 脚本扩写
- 分镜拆解
- 视觉风格统一
- 提示词优化
- vibecoding 检测
- 平台功能修改

判断之后，再选择合适的知识、记忆、模型或工具。

### 3. 创作知识调度

知识调度应围绕真实创作场景。

例如：

- 用户问怎么开始广告短片：调用平台工作流和创作流程知识
- 用户问怎么拆分镜：调用影视创作和镜头语言知识
- 用户问怎么更高级：调用视觉设计知识
- 用户问结果不稳定：调用提示词优化经验
- 用户问代码在哪里改：切换到开发协作视角

### 4. 创作记忆

记忆用于保持项目连续性。

- 短期记忆：当前对话刚刚发生的事
- 中期记忆：当前项目阶段和最近确定的方向
- 长期记忆：用户长期偏好和协作方式

### 5. 输出去向

最终结果可以输出到：

- 右侧助手面板
- 文本节点
- 图片节点
- 视频节点
- 项目文件
- 架构说明文档

---

## 临时结论

如果是给别人看，建议以版本 B 为主，因为它更符合 VideoStudio Pro 的产品调性。

如果是给自己梳理系统，版本 A 更清楚，因为它把输入层、决策层、执行层拆得更完整。

后续可以把两者合并成最终版：

```text
用版本 B 的平台语言
+ 保留版本 A 的结构清晰度
```
